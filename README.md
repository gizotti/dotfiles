# dotfiles

## Installation

``` sh
FRESH_LOCAL_SOURCE=gizotti/dotfiles bash <(curl -sL get.freshshell.com)
```

My dotfiles are managed by [fresh](http://freshshell.com).

git config --global buildkite.apikey $BUILDKITE_SECRET

export GPG_TTY=$(tty)
git config --global user.signingkey $GPGKEY
